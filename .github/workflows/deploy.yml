# Disabled — using Jenkins for deploy instead of GitHub Actions.
# Rename back to deploy.yml if you want Actions again.

name: Deploy AI Marketing (disabled)

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions deploy disabled. Use Jenkins pipeline."
